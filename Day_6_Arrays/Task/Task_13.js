//Access and log a specific element from the two-dimensional array.

// Define a two-dimensional array (matrix)
let matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
];

// Specify the row and column indices
let i = 1; // second row (index starts from 0)
let j = 2; // third column (index starts from 0)

// Access and log the specific element
console.log(matrix[i][j]); // Output: 6
